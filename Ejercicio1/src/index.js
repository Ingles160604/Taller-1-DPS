import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Puedes tener un archivo CSS separado para estilos adicionales
import App from './App';
import reportWebVitals from './reportWebVitals';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

reportWebVitals();
